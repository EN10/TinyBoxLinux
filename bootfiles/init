#!/bin/sh

# Suppress kernel messages
dmesg -n 1

# Mount file systems
mkdir proc sys tmp
mount -t proc none /proc
mount -t sysfs none /sys
mount -t tmpfs none /tmp

# Setup networking
ip link set eth0 up
udhcpc -b -i eth0 -s /etc/network.sh

# Start shell
/bin/sh
